"""The v1 routers, one per section of ``docs/WEBSERVICE_API.md``.

Every endpoint wraps exactly one public ``doe`` function; stubs return 501 until their
milestone lands (``docs/WEBSERVICE_BUILD.md``).
"""
